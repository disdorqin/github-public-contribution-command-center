# 2026-06-21

## feat(contrib-center): GitHub Public Contribution Command Center v1 (Safe Mode)

Built and committed a brand-new `src/contrib_center/` module on top of an
upstream `SWE-agent/mini-swe-agent` clone (moshevels4 fork not yet
created on github.com — used upstream + future rebase).

### What was built
- `src/contrib_center/` with public-only visibility guard (the security
  keystone — fail-closed on private/internal/unknown), `gh` CLI wrapper,
  YAML config loader, SQLite state, JSONL logger, 6-dimension scoring
  engine, 6 bots (profile, own_repo, issue_scout, pr_agent, report,
  notification) and 5 adapters (mini_swe subprocess wrapper, plus stubs
  for lowlighter/metrics, Platane/snk, github-readme-stats, PR-Agent).
- `config/{public_repos,rules,external_search,profile,prompts}.yml`
- `.github/workflows/daily-contribution-center.yml` — 09:00 SGT cron
- `data/` and `reports/` directories with append-only jsonl/sqlite
- `tests/test_visibility_guard.py` + `tests/test_scoring.py` (10 tests,
  all passing)
- `README_CONTRIB_CENTER.md` with token, local run, GH Actions, and
  Safe→Assisted upgrade instructions

### Key safety contract
- All GitHub ops go through `visibility_guard.guard_repo_operation()`
- Safe Mode: never publishes external PRs/comments/pushes
- 0 private-repo touches; verified by 6 unit tests + end-to-end CLI run

### Verification
- `pytest tests/test_visibility_guard.py tests/test_scoring.py` → 10 passed
- `python -m contrib_center.main check-visibility moshevels4/DARIS` →
  correctly refused with `[SECURITY_SKIP] reason=not_public` (gh CLI not
  logged in → fail-closed)
- `python -m contrib_center.main run --mode safe` → end-to-end pipeline
  ran, generated `reports/2026-06-21.md` + `data/{action_log,rejected}.jsonl`,
  refused all 4 whitelisted repos (gh not authed), produced 0 external
  PRs/comments (Safe Mode)

### Open items
1. **moshevels4 GitHub account does not exist** (404 on github.com). I
   built the system against upstream SWE-agent/mini-swe-agent. The user
   needs to (a) create the account, (b) fork the upstream, then (c)
   rebase the `feat/contrib-center` branch onto their fork. **Tell the
   user to rotate the PAT that was pasted in chat** (it was leaked).
2. A GitHub PAT was pasted in chat earlier. **The user must
   rotate this token immediately** before deploying the workflow.
3. Profile repo name: user chose `moshevels4/dis_daily_agent` as the
   GitHub Profile README repo. The whitelist supports both
   `moshevels4/moshevels4` and `moshevels4/dis_daily_agent` as profile
   repos (whichever one the user actually sets up).
4. Branch `feat/contrib-center` has 1 commit on top of upstream HEAD
   `674e908`. No push to any remote was attempted.

### Things that surprised me
- `rm -rf` on the work dir accidentally wiped the `_preserve` backup
  (was in the same command). Lost 30 files once, had to re-clone
  upstream and re-write everything. Now keeping a stricter mental rule:
  **never include the preserve dir in the same `rm -rf` invocation as
  the dirs to be removed**.
- `gh search issues` returns rc=4 when not authed (vs rc=127 when not
  installed) — both treated as fail-closed, but the log shows
  different `source` values.

---

## Safe Mode v1.1 Hardening (Second Round)

### Issue Body Reading Fix
- **Problem**: External issue `body` was not being read, only `title` was checked for high-risk keywords
- **Solution**: Added `get_issue_body()` function to `github_client.py` that fetches full issue body using `gh issue view <url> --json body --jq .body`
- **Updated**: `issue_scout.py` to fetch body if not present in search results

### High-Risk Keyword Strengthening
- **Added keywords**: auth, access token, api key, token leak
- **Updated**: `visibility_guard.py` and `scoring.py` deny keyword lists
- **Verification**: Both `title + body` are now checked

### Visibility Guard Tests Completed
- **Added**: 9 new tests to `test_visibility_guard.py` (15 total)
- **Coverage**:
  - Own repo in allowlist + public → pass
  - Own repo not in allowlist → reject
  - Own private repo → reject (even if in allowlist)
  - External public repo read/clone/generate_patch → pass
  - External push/create_pr/comment → reject
  - External private/unknown repo → reject

### Issue Scout Tests Created
- **Added**: `tests/test_issue_scout.py` with 8 tests
- **Coverage**:
  - Issue body with private key → skipped
  - Issue body with credential → skipped
  - External repo must prove public
  - Safe Mode write_allowed always false
  - Title and body both checked for deny keywords

### mini_swe_adapter Safety Verification
- **Verified**: No `git push`, `gh pr create`, or `gh issue comment` commands
- **Confirmed**: Only local patch drafts are generated, nothing is published

### Test Results
- ✅ All 27 tests pass (15 visibility_guard + 4 scoring + 8 issue_scout)
- ✅ Safe Mode enforces all security rules correctly

### Commit
- **Hash**: `73a8db28`
- **Message**: "fix: harden external issue filtering and guard tests"
- **Branch**: `feat/contrib-center`
- **Pushed to**: `disdorqin/github-public-contribution-command-center`

### Security Rules Verified
- ✅ Issue body is now read and checked for high-risk keywords
- ✅ Title + body both checked
- ✅ External public repos: read/clone/generate_patch allowed
- ✅ External push/create_pr/comment: rejected
- ✅ Private/internal repos: fully rejected
- ✅ No token leakage in code/logs
- ✅ Safe Mode: no external PRs/comments/publishes

---

## Autopilot Mode Implementation (Third Round)

### Completed Tasks
- **Task 29-34**: Implemented complete autopilot system
  - Fixed `mini_swe_adapter.py` to persist patches to `data/patches/YYYY-MM-DD/`
  - Modified `external_pr_publisher.py` to support `patch_file` parameter
  - Created `config/autopilot.yml` for autopilot configuration
  - Created `src/contrib_center/autopilot_publisher.py` for automatic PR publishing
  - Modified `orchestrator.py` to implement complete daily flow
  - Modified `.github/workflows/daily-contribution-center.yml` to support autopilot mode
  - Modified `src/contrib_center/main.py` to add `--patch-file` and `autopilot-publish` commands

### Test Files Created
- **tests/test_autopilot_publisher.py**: 21 tests
  - TestLoadPatchMetadata: 3 tests
  - TestLoadPublishedPRs: 3 tests
  - TestCanPublishToday: 3 tests
  - TestCheckCooldown: 3 tests
  - TestIsSafeCandidate: 5 tests
  - TestAutopilotPublishOne: 4 tests

- **tests/test_mini_swe_patch_persistence.py**: 7 tests
  - TestPatchResult: 2 tests
  - TestCountDiffLines: 3 tests
  - TestGitDiff: 1 test
  - TestGeneratePatchPersistence: 1 test

### Test Results
- ✅ All 28 tests pass (21 + 7)
- ✅ Fixed 6 failing tests by properly mocking `guard_external_public_repo_read`
- ✅ Fixed `PublishResult` instantiation with required `upstream_repo` parameter

### Security Features Implemented
- Max 1 PR per day
- Max 1 PR per run
- Only low-risk issues (docs, tests, typos)
- No security/auth/payment/credential issues
- No private/internal repos
- Cooldown rules (72h same repo, 24h same owner)
- Patch limits (max 5 files, 200 diff lines)
- PR body must disclose AI assistance

### Commit & Push
- **Commit hash**: f080c1c2
- **Message**: "feat: Implement autopilot mode for automatic external PR publishing"
- **Files changed**: 9 files, 1359 insertions(+), 77 deletions(-)
- **Pushed to**: `disdorqin/github-public-contribution-command-center` main branch

### Operation Modes
- `safe`: Default for cron, no external PRs
- `assisted`: Manual trigger with confirm_publish=true
- `autopilot`: Automatic but restricted to 1 PR/day

### Next Steps
- User needs to test the autopilot mode in real GitHub Actions environment
- Monitor the first automatic PR creation
- Adjust `config/autopilot.yml` settings based on real-world results
